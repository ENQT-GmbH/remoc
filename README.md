# Channel multiplexing and remote procedure calls (RPC)

This library allows to multiplex one channel into multiple logical channels.
On top of that remote procedure calls with message passing is implemented.

